apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  labels:
    app: netflix-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
    - port: 9100
      targetPort: 9100
  selector:
    app: netflix-app
